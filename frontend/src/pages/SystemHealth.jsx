import React, { useEffect, useState } from 'react';
import { Activity, Database, HardDrive, RefreshCw, ShieldCheck } from 'lucide-react';
import api from '../api/client';
import { getApiErrorMessage } from '../utils/errors';
const bytes=value=>{if(value==null)return'—';const units=['B','KB','MB','GB','TB'];let index=0,number=value;while(number>=1024&&index<units.length-1){number/=1024;index++}return`${number.toFixed(index?1:0)} ${units[index]}`};
export default function SystemHealth(){const[data,setData]=useState(null);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const load=async()=>{setLoading(true);try{setData((await api.get('/system-health')).data);setError('')}catch(err){setError(getApiErrorMessage(err))}finally{setLoading(false)}};useEffect(()=>{load();const timer=setInterval(load,15000);return()=>clearInterval(timer)},[]);if(!data&&!error)return<div className="analytics-state">Loading system health…</div>;return<div><div className="page-title"><div><h2>System Health</h2><p>Operational health of the NetMonitor platform and its active database.</p></div><button className="btn btn-secondary" onClick={load}><RefreshCw size={15} className={loading?'spin':''}/> Refresh</button></div>{error&&<div className="notice error">{error}</div>}{data&&<><div className="metric-summary-grid"><Health icon={ShieldCheck} label="Platform" value={data.status}/><Health icon={Database} label="Database" value={`${data.database.latency_ms} ms`} note={data.database.dialect}/><Health icon={Activity} label="Monitoring cycle" value={data.monitoring_engine.last_cycle_duration_seconds==null?'Waiting':`${data.monitoring_engine.last_cycle_duration_seconds.toFixed(2)} s`} note={`${data.monitoring_engine.completed_cycles} completed · ${data.monitoring_engine.failed_cycles} failed`}/><Health icon={HardDrive} label="Disk available" value={bytes(data.storage.disk_free_bytes)} note={`${bytes(data.storage.disk_used_bytes)} used`}/></div><div className="settings-grid"><section className="glass-card settings-panel"><h3>Runtime</h3><dl className="device-facts"><Row label="Engine" value={data.monitoring_engine.running?'Running':'Stopped'}/><Row label="Last successful cycle" value={data.monitoring_engine.last_cycle_completed_at?new Date(data.monitoring_engine.last_cycle_completed_at).toLocaleString():'Not completed'}/><Row label="WebSocket clients" value={data.runtime.websocket_connections}/><Row label="Running discovery jobs" value={data.runtime.discovery_jobs_running}/><Row label="Process maximum RSS" value={data.storage.process_max_rss_kb==null?'Unavailable on this operating system':`${data.storage.process_max_rss_kb.toLocaleString()} KB`}/></dl></section><section className="glass-card settings-panel"><h3>Stored operations</h3><dl className="device-facts"><Row label="Raw metric samples" value={data.counters.raw_metrics.toLocaleString()}/><Row label="Metric aggregates" value={data.counters.metric_aggregates.toLocaleString()}/><Row label="Active alerts" value={data.counters.active_alerts.toLocaleString()}/><Row label="Notification deliveries" value={data.counters.notification_deliveries.toLocaleString()}/><Row label="Active sessions" value={data.counters.sessions.toLocaleString()}/></dl></section></div>{data.monitoring_engine.last_cycle_error&&<div className="notice error">Monitoring cycle error: {data.monitoring_engine.last_cycle_error}</div>}</>}</div>}
function Health({icon:Icon,label,value,note}){return<div className="metric-summary-card"><Icon/><span>{label}</span><strong>{value}</strong>{note&&<small>{note}</small>}</div>}function Row({label,value}){return<div><dt>{label}</dt><dd>{value}</dd></div>}
