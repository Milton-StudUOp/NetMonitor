{
  "name": "netmonitor-whatsapp-web",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated whatsapp-web.js notification bridge for NetMonitor",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:local": "env -u WHATSAPP_WEB_SERVICE_TOKEN -u PUPPETEER_EXECUTABLE_PATH node --env-file=../.env src/server.js",
    "start:qr": "env -u WHATSAPP_WEB_SERVICE_TOKEN -u PUPPETEER_EXECUTABLE_PATH WHATSAPP_PRINT_QR_TERMINAL=true node --env-file=../.env src/server.js",
    "install-browser": "puppeteer browsers install chrome"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "5.1.0",
    "qrcode": "1.5.4",
    "qrcode-terminal": "0.12.0",
    "whatsapp-web.js": "1.34.7"
  }
}
